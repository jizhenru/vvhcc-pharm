<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.vvcs.pharm.dao.RoleMapper">
<!--  <insert id="addRole" parameterType="com.vvcs.pharm.pojo.Role">
 insert into vvcs_role (rolename) values(#{rolename})
 </insert> -->
 <select id="selectRole" resultType="com.vvcs.pharm.pojo.Role">
 select * from vvcs_role
 </select>
  <select id="selectRoleId" resultType="com.vvcs.pharm.pojo.Role">
 select * from vvcs_role where id=#{id}
 </select>

 
</mapper>